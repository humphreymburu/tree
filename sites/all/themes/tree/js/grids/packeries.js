(function ($) {
Drupal.behaviors.addIsotopeClass = {
  attach: function(context, settings) {
    var play = $('.isotope-element');
    //$(play[0]).addClass('one');
    //$(play[1]).addClass('two');  
	
  }
};
})(jQuery);